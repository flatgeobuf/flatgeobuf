namespace FlatGeobuf;

enum ColumnType:ubyte {
  BYTE,
  UBYTE,
  BOOL,
  SHORT,
  USHOT,
  INT,
  UINT,
  FLOAT,
  LONG,
  ULONG,
  DOUBLE,
  STRING,
  JSON
}

table Column {
  name: string (required);
  type: ColumnType;
}

root_type Column;