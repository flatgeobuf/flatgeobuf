include "geometry.fbs";
include "column.fbs";

namespace FlatGeobuf;

table Feature {
  geometry: Geometry;
  columns: [Column];
  column_indexes: [uint];
  values: [Value];
}

table Value {
  data: [ubyte];
}

root_type Feature;
