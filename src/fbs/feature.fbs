include "geometry.fbs";
include "column.fbs";

namespace FlatGeobuf;

table Feature {
  geometry: Geometry;
  columns: [Column];
  columns_indexes: [uint];
  values_lengths: [uint];
  values: [ubyte];
}

table Value {
  data: [ubyte];
}

root_type Feature;
