namespace FlatGeobuf;

enum GeometryType:ubyte {
  Point,
  MultiPoint,
  LineString,
  MultiLineString,
  Polygon,
  MultiPolygon,
  GeometryCollection
}

table Geometry {
  type: GeometryType;
  geometries: [Geometry]; // GeometryCollection
  envelope: [double];
  dimensions: ubyte = 2;
  endss: [uint]; // MultiPolygon part ends
  ends: [uint]; // MultiLineString part ends or Polygon ring ends
  coords: [double];
}

root_type Geometry;