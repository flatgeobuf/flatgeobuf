include "column.fbs";
include "geometry.fbs";
include "feature.fbs";

namespace FlatGeobuf;

table Header {
  name: string;
  envelope: [double];
  geometry_type: GeometryType;
  dimensions: ubyte = 2;
  columns: [Column];
  features_count: ulong;
  features_size: ulong;
}

root_type Header;
